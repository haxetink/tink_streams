{
  "name": "tink_streams",
  "version": "0.0.0",
  "description": "",
  "scripts": {
    "travix": "neko travix.n",
    "postinstall": "lix download && haxe -lib travix -main travix.Travix -neko travix.n && npm install -g switchx && switchx"
  },
  "dependencies": {
    "lix": "=15.0.0-rc.2"
  }
}
